# Docker 镜像打包与替换指南

## 项目镜像一览

| 镜像 | 构建目录 | 说明 |
|------|---------|------|
| `ski-backend:latest` | `backend/` | Spring Boot 后端，Maven 多阶段构建 |
| `ski-ai-engine:latest` | `ai-engine/` | Python AI 引擎，含 YOLO/ResNet50 模型 |
| `ski-frontend:latest` | `frontend/` | Vue 前端，Nginx 静态服务 |

## 一、判断哪些镜像需要重新打包

只打包**有改动**的镜像。对照 git diff 判断：

```
git diff --name-only
```

- `backend/*` 有改动 → 重新打包 **ski-backend**
- `ai-engine/*` 有改动 → 重新打包 **ski-ai-engine**
- `frontend/*` 有改动 → 重新打包 **ski-frontend**

没改动的镜像不需要重新打包，客户电脑上已有的继续用。

---

## 二、在自己电脑上打包

### 2.1 构建镜像

```bash
# 打包所有有改动的镜像（示例：ai-engine 和 frontend 有改动）
docker compose build ai-engine frontend

# 如果三个都有改动
docker compose build backend ai-engine frontend
```

### 2.2 导出为 .tar 文件

```bash
docker save -o ski-ai-engine.tar ski-ai-engine:latest
docker save -o ski-frontend.tar ski-frontend:latest
```

### 2.3 传输到客户电脑

用 U 盘、网盘或 `scp` 把 `.tar` 文件拷贝过去。

---

## 三、在客户电脑上替换

### 3.1 加载新镜像

```bash
docker load -i ski-ai-engine.tar
docker load -i ski-frontend.tar
```

> 加载同 tag 的镜像会自动覆盖旧版本（旧镜像变为 `<none>:<none>`）。

### 3.2 重建并重启容器

```bash
# 只重建有改动的服务，其他容器不受影响
docker compose up -d --force-recreate ai-engine frontend
```

### 3.3 验证

```bash
# 确认容器都在运行
docker compose ps

# 确认新镜像已生效
docker images ski-ai-engine ski-frontend
```

---

## 四、注意事项

1. **volume 数据不会丢失**：`docker compose up --force-recreate` 只重建容器，挂载的 volume（MySQL 数据、Redis 数据、AI 模型、ChromaDB 数据）全部保留。
2. **backend 无需重启就不会中断**：如果 backend 没改，只重建 ai-engine/frontend，backend 服务不会中断。
3. **清理旧镜像**：加载新镜像后，旧镜像 tag 会变成 `<none>`，可在客户电脑上执行以下命令清理：
   ```bash
   docker image prune -f
   ```
4. **模型文件较大**：`ski-ai-engine` 镜像包含预下载的 YOLO/ResNet50 模型，`.tar` 文件可能 2-5GB，注意传输耗时。
5. **环境变量**：如果 `.env` 文件或 `docker-compose.yml` 中的环境变量有改动，需要一并更新客户电脑上的文件，然后重建相关容器。
